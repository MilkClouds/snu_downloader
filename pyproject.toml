[project]
name = "snu-downloader"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "MilkClouds", email = "milkclouds@snu.ac.kr" }]
dependencies = [
  "selenium",
  "requests",
  "tqdm",
  "yt-dlp",
]

[dependency-groups]
dev = ["ruff"]

[tool.uv]
# uv.lock is not committed; bump this date to upgrade dependencies reproducibly.
exclude-newer = "2026-09-07T00:00:00Z"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
bypass-selection = true

[tool.ruff]
line-length = 119

